import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    name: {
        marginRight: "1rem",
    },
    email: {
        marginRight: "1rem",
    },
    btn: {
        color: "#a8aaff",
        fontWeight: "700",
        fontSize: "1.25rem",
        marginTop: ".70rem",
        cursor: "pointer",
    },
}));
